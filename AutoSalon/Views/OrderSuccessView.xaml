<Window x:Class="AutoSalon.Views.OrderSuccessView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoSalon.Views"
        mc:Ignorable="d"
        Title="OrderSuccessView" Height="300" Width="300">
    <Window.Background>
        <ImageBrush ImageSource="\Resources\succsess_order.jpg"/>
    </Window.Background>
    <Window.Resources>
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Montserrat"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1f0854"/>
        </Style>
    </Window.Resources>
    <Grid>
        <TextBlock HorizontalAlignment="Right" Style="{StaticResource TitleStyle}" Margin="0,0,20,20" TextWrapping="Wrap" Text='Заказ сформирован' VerticalAlignment="Center"/>
    </Grid>
</Window>
